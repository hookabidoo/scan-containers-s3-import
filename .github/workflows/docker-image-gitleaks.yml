name: build-and-push-docker-images-gitleaks

on:
  push:
    branches:
      - "main"
    paths:
      - 'gitleaks/**'

jobs:
  call-workflow-passing-data:
    uses: ./.github/workflows/docker-image-reusable.yml@main
    with:
      imagename: gitleaks
    secrets: inherit
